<div class="callout secondary">
  <% @users.each do |user| %>
    <div class="callout">
      <li><%= link_to "#{user.full_name}", user %></li>
      <p>(role: <%= user.role %>)</p>
    </div>
  <% end %>
</div>
