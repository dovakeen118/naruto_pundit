<div class="callout">
  <h1>All Users (<%= @users.length %>)</h1>

  <% if current_user && current_user.admin? %>
    <%= render partial: 'admin_user_view', users: @users %>
  <% else %>
    <%= render partial: 'user_view', users: @users %>
  <% end %>

</div>
